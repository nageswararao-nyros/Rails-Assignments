	<div class="container">
		<h1 align="center"><%= @article.title %></h1>

		<table class="table table-bordered bg-light">
    	<tr>
				<td>
					<%= image_tag @article.poster.url, class:"image-size" %>
				</td>
			</tr>	

			<tr>
				<td colspan="2">
					<%= @article.story %>
				</td>
			</tr>		
		</table>
		<%= link_to 'Back', articles_path , {:class=>'btn btn-warning'} %>
	</div>
